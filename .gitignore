# Gradle / build
.gradle/
build/
app/build/
captures/
.cxx/

# Local environment
local.properties
*.log

# IDE
.idea/
*.iml
.DS_Store

# Keys — the bundled dev keystore is intentionally tracked (see README);
# real publishing keys must never be.
keystore/*.keystore
release-keys/
